#include <iostream>
#include <string>

class Person {
private:
    std::string name;
    static int population;

public:
    Person(std::string n) {
        name = n;
        population++;
    }

    static void print() {
        std::cout << name << population;
    }
}